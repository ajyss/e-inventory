# API Documentation (placeholder)

Dokumentasi ini berisi daftar endpoint backend-api, contoh request/response, dan informasi autentikasi.

Silakan isi dengan endpoint nyata dari `e-inventory/backend-api`.

Contoh struktur:

- **POST** `/api/auth/login` — Login dan dapatkan token
  - body: `{ "email": "user@example.com", "password": "secret" }`
  - response: `{ "token": "..." }`

- **GET** `/api/products` — Ambil daftar produk (support pagination & filter)

Tambahkan endpoint lain di bawah ini sesuai implementasi.
